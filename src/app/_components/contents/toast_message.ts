// app-failure-toast-messages.ts

export const AppFailureToastMessages = Object.freeze({
  ASSIGNMENT_CARD_FETCH:
    "Unable to fetch assignment statistics",
     STUDENT_DASHBOARD_COUNT_FETCH:
    "Unable to fetch course overview data",
    MEETING_FETCH:
    "Unable to fetch upcoming meeting details",
  JOIN_MEETING:
    "Unable to join meeting",
     RESCHEDULE_SUBMIT:
    "Unable to submit reschedule request",
});